<h1>Add a review</h1>

<%= simple_form_for [@user, @review] do |f| %>
        <%= f.input :rating, collection: 0..5 %>
        <%= f.input :content %>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>

